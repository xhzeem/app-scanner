# App-Scanner Environment Variables
# Copy to .env and adjust for your environment.

# ─── Logging ──────────────────────────────────────────────────
# Log verbosity: debug, info, warn, error (default: info)
LOG_LEVEL=info

# ─── API ──────────────────────────────────────────────────────
# Sirius API base URL (default: http://localhost:9001)
SIRIUS_API_URL=http://localhost:9001

# ─── Message Queue ────────────────────────────────────────────
RABBITMQ_HOST=localhost
RABBITMQ_PORT=5672
RABBITMQ_QUEUE=sirius-scan

# ─── Key-Value Store (Valkey / Redis-compatible) ──────────────
VALKEY_ADDR=localhost:6379

# ─── NSE Scripts ──────────────────────────────────────────────
# Base path for NSE script repository (default: /opt/sirius-nse)
NSE_BASE_PATH=/opt/sirius-nse
# Git URL for the NSE script repository (default: https://github.com/SiriusScan/sirius-nse.git)
NSE_REPO_URL=https://github.com/SiriusScan/sirius-nse.git

# ─── Scanning Tools ──────────────────────────────────────────
# Path to nmap binary (default: uses system PATH)
# NMAP_PATH=/usr/bin/nmap
